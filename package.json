{
  "name": "resume-builder",
  "version": "0.1.0",
  "private": true,
  "description": "AI-Powered Resume Tailoring Engine. See frontend/ for the Next.js app.",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run start",
    "graphify": "node scripts/generate_graph.js"
  }
}
